# TR_NPCGenerator
TR_NPCGenerator

tes3cmd command to dump npc info - 0x001 is female flag - use bitwise logic to separate into separate files

tes3cmd dump --type npc_ --format "%name%|%race%|%flags%" sky_main.esm > SKY_npcs.txt
